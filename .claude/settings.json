{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": []
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit *)",
        "hooks": [
          {
            "type": "command",
            "command": "printf '{\"systemMessage\":\"Before committing, run the middleware-code-review skill, complete its strict pre-commit checklist, and rerun relevant tests/checks.\",\"continue\":false,\"stopReason\":\"Run middleware-code-review and complete its checklist before git commit.\"}'",
            "timeout": 10,
            "statusMessage": "Enforcing pre-commit code review"
          }
        ]
      }
    ]
  }
}