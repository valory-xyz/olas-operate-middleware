Use the `explore-middleware-codebase` skill for this workflow.

This command is retained as a thin compatibility entrypoint.
The skill is the single source of truth for middleware-specific planning guidance.
